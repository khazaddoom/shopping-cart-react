import React, { useContext } from 'react'

function Housing() {
    const context = useContext(shoppingContext)
    return (
        <div>
            This is Housing!!!
        </div>
    )
}

export default Housing
